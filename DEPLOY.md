# Deploy (one command each)

## Vercel
```bash
npx vercel --prod
```

## Netlify
```bash
npx netlify deploy --prod --dir .
```

## GitHub Pages
```bash
git push origin main
```

> Configure repository settings once: **Pages → Deploy from branch → main / root**.
